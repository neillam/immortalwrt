name: Build AX6000 Closed-Source Final

on:
  workflow_dispatch:

env:
  # 源码锁定 hanwckf 分支，确保闭源驱动稳定
  REPO_URL: https://github.com/hanwckf/immortalwrt-mt798x
  REPO_BRANCH: openwrt-21.02

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 彻底清理磁盘空间
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true

      - name: 安装编译依赖
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential clang flex bison g++ gawk gcc-multilib g++-multilib \
            gettext git libncurses5-dev libssl-dev python3-setuptools rsync swig unzip zlib1g-dev \
            file wget libelf-dev lz4 zstd
          sudo apt-get clean

      - name: 克隆源码
        run: |
          git clone --depth 1 $REPO_URL -b $REPO_BRANCH openwrt
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 强制生成 .config 配置文件
        working-directory: ./openwrt
        run: |
          # 1. 核心：ID 必须完全匹配您查到的 DTS 文件名 (带 -stock)
          echo "CONFIG_TARGET_mediatek=y" > .config
          echo "CONFIG_TARGET_mediatek_filogic=y" >> .config
          echo "CONFIG_TARGET_mediatek_filogic_DEVICE_xiaomi_redmi-router-ax6000-stock=y" >> .config
          
          # 2. 闭源驱动核心组件
          echo "CONFIG_PACKAGE_mtk-wifi-mt7986=y" >> .config
          echo "CONFIG_PACKAGE_kmod-mt7986-firmware=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-mtkwifi=y" >> .config
          
          # 3. 硬件加速与中文语言包
          echo "CONFIG_PACKAGE_kmod-mtk-offload=y" >> .config
          echo "CONFIG_PACKAGE_luci=y" >> .config
          echo "CONFIG_PACKAGE_luci-base=y" >> .config
          echo "CONFIG_PACKAGE_luci-i18n-base-zh-cn=y" >> .config
          
          # 4. 其它必要工具
          echo "CONFIG_PACKAGE_curl=y" >> .config
          echo "CONFIG_PACKAGE_htop=y" >> .config
          
          # 补全所有依赖，此时不应出现 openwrt_one 或 spim-nor 的回退提示
          make defconfig

      - name: 预下载依赖包
        working-directory: ./openwrt
        run: |
          make download -j8
          find dl -size -1k -exec rm -f {} \;

      - name: 编译固件
        working-directory: ./openwrt
        run: |
          # 优先多线程
          make -j$(nproc) || make -j1 V=s

      - name: 整理并搜寻 NAND 产物
        if: always()
        run: |
          mkdir -p ./artifact/
          # 专门寻找带 ax6000 且符合 NAND 布局的镜像
          find openwrt/bin/targets/ -type f -name "*ax6000*.ubi" -exec cp {} ./artifact/ \;
          find openwrt/bin/targets/ -type f -name "*ax6000*sysupgrade.bin" -exec cp {} ./artifact/ \;
          # 打印列表，确认是否有 factory.ubi
          echo "--- 搜寻到的产物列表如下 ---"
          ls -R ./artifact/

      - name: 上传固件产物
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: AX6000-ClosedSource-NAND-Result
          path: ./artifact/
